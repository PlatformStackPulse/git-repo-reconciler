name: check
description: GitHub composite action to run lint, test, and security checks

runs:
  using: composite
  steps:
    - name: Install dependencies
      shell: bash
      run: |
        sudo apt-get update && sudo apt-get install -y shellcheck bats

    - name: Run checks
      shell: bash
      run: |
        make lint test security
