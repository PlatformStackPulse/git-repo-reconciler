name: build
description: GitHub composite action to build bash-template

runs:
  using: composite
  steps:
    - name: Build
      shell: bash
      run: |
        chmod +x scripts/build.sh
        make build
