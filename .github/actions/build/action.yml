name: build
description: GitHub composite action to build git-repo-reconciler (grr)

runs:
  using: composite
  steps:
    - name: Build
      shell: bash
      run: |
        chmod +x scripts/build.sh
        make build
